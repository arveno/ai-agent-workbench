# Brand Logo Assets

Please place the real brand logos (SVG) in this directory with the exact file names below:

- `mock.svg`
- `groq.svg`
- `gemini.svg`
- `openrouter.svg`
- `openai.svg`
- `ollama.svg`

Notes:

- This repository only ships `mock.svg` by default.
- If a brand file is missing, the UI falls back to text in the model option icon box.
- To strictly align with the design, replace missing files with official brand SVG assets.
